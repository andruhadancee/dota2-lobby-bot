# 🚀 Инструкция по обновлению бота на VPS

## Шаг 1: На компьютере (локально)

Запустите файл **`push_changes.bat`**:

```cmd
push_changes.bat
```

Или вручную в PowerShell:
```powershell
cd "C:\Users\lisag\Desktop\бот для создания лобби"
git add dota2_real_lobby_bot_v2.py
git commit -m "fix: add return on lobby creation timeout + increase wait time"
git push
```

Это отправит изменения в Git репозиторий.

---

## Шаг 2: На VPS (Termius)

### Вариант A: Использовать скрипт update_vps.sh

**⚠️ ВНИМАНИЕ:** Этот скрипт **останавливает** старого бота с помощью `pkill -9 python3`.

Подключитесь к VPS через Termius и выполните:

```bash
cd ~/dota2_lobby_bot
bash update_vps.sh
```

Скрипт автоматически:
1. Остановит старого бота
2. Скачает обновления (`git pull`)
3. Обновит зависимости
4. Запустит нового бота

---

### Вариант B: Обновление вручную (без остановки)

Если хотите обновить без полной остановки:

```bash
cd ~/dota2_lobby_bot

# Скачайте изменения
git pull

# Найдите процесс бота
ps aux | grep "python.*main.py"

# Отправьте SIGTERM для корректного завершения
kill -TERM <PID>

# Подождите 5-10 секунд

# Запустите нового бота
source venv/bin/activate
python main.py
```

---

## ✅ Проверка обновления

После обновления проверьте логи:

```bash
tail -f dota2_real_bot.log
```

Или проверьте что бот работает в Telegram.

---

## 🔄 Что делать если что-то пошло не так

### Бот не запустился:

```bash
cd ~/dota2_lobby_bot
source venv/bin/activate
python main.py
```

### Проверить установлены ли зависимости:

```bash
pip list | grep -E "steam|telegram|dotenv"
```

Если чего-то не хватает:
```bash
pip install -r requirements.txt
```

### Если нужен быстрый откат:

```bash
cd ~/dota2_lobby_bot
git log --oneline -5  # Посмотреть последние 5 коммитов
git checkout <предыдущий_коммит>  # Откатиться
source venv/bin/activate
python main.py
```

---

## 📝 Краткая памятка

**Локально:**
```cmd
push_changes.bat
```

**На VPS:**
```bash
cd ~/dota2_lobby_bot
bash update_vps.sh
```

**Или вручную:**
```bash
cd ~/dota2_lobby_bot
git pull
source venv/bin/activate
python main.py
```

