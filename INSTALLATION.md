# 🚀 Инструкция по установке и запуску

## Шаг 1: Установка Python

Убедитесь, что у вас установлен Python 3.8 или выше:
```bash
python --version
```

## Шаг 2: Настройка файла .env

Создайте файл `.env` в корне проекта и заполните его:

1. **Получите токен бота:**
   - Откройте Telegram
   - Найдите @BotFather
   - Отправьте `/newbot`
   - Следуйте инструкциям
   - Скопируйте токен бота

2. **Узнайте свой Telegram ID:**
   - Откройте @userinfobot в Telegram
   - Нажмите `/start`
   - Скопируйте число из "Id: XXXXXXX"

3. **Заполните .env:**
   ```env
   TELEGRAM_BOT_TOKEN=ваш_токен_бота
   ADMIN_IDS=ваш_telegram_id
   NOTIFICATION_CHAT_ID=
   NOTIFICATION_THREAD_ID=
   ```

## Шаг 3: Установка зависимостей

В корневой директории проекта выполните:

**Windows:**
```bash
pip install -r requirements.txt
```

**Linux/Mac:**
```bash
pip3 install -r requirements.txt
```

**Или в виртуальном окружении:**
```bash
# Создание виртуального окружения
python -m venv venv

# Активация (Windows)
venv\Scripts\activate

# Активация (Linux/Mac)
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt
```

## Шаг 4: Добавление Steam аккаунтов

Аккаунты уже должны быть в `steam_accounts.json`, но если нужно добавить:

**Через бота (после запуска):**
1. Отправьте `/start`
2. Нажмите "🤖 Управление ботами"
3. Нажмите "➕ Добавить бота"
4. Введите: `логин пароль`

**Или вручную в файл:**
```json
[
  {
    "username": "ваш_логин",
    "password": "ваш_пароль"
  }
]
```

⚠️ **ВАЖНО:** 
- Аккаунты должны иметь доступ к Dota 2
- Steam Guard должен быть отключен

## Шаг 5: Запуск бота

### Windows:
```bash
python main.py
```

### Linux/Mac:
```bash
python3 main.py
```

### В виртуальном окружении:
```bash
# Активируйте окружение (см. Шаг 3)
python main.py
```

## Шаг 6: Проверка работы

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Должно появиться меню с кнопками

## 🔧 Возможные проблемы

### "Нет TELEGRAM_BOT_TOKEN"
- Проверьте, что файл `.env` существует в корне проекта
- Проверьте правильность токена

### "❌ Нет доступа"
- Проверьте `ADMIN_IDS` в `.env`
- Узнайте ваш ID через @userinfobot

### "Не удалось войти в Steam"
- Проверьте логин/пароль аккаунта
- Убедитесь, что Steam Guard отключен
- Проверьте доступ к Dota 2

### Ошибки установки зависимостей
Для Windows может потребоваться:
```bash
python -m pip install --upgrade pip
pip install wheel
pip install -r requirements.txt
```

Для Linux может потребоваться:
```bash
sudo apt-get install python3-dev
sudo apt-get install python3-pip
```

## 📝 Логи

Все логи сохраняются в файл:
```
dota2_real_bot.log
```

## 🛑 Остановка бота

Нажмите `Ctrl+C` в консоли. Бот корректно закроет все активные лобби перед остановкой.

## 🔄 Обновление

```bash
git pull
pip install -r requirements.txt --upgrade
python main.py
```

## 📁 Структура файлов

```
бот для создания лобби/
├── main.py                      # Точка входа
├── dota2_real_lobby_bot_v2.py   # Основной код
├── requirements.txt             # Зависимости
├── .env                         # Конфигурация (создайте!)
├── steam_accounts.json          # Аккаунты Steam
├── schedule_config.json         # Расписание
├── lobby_settings.json          # Настройки (создается автоматически)
├── dota2_real_bot.log          # Логи (создается автоматически)
├── README.md                    # Документация
├── INSTALLATION.md              # Эта инструкция
└── __pycache__/                 # Кэш Python
```

## 🎮 Использование

После запуска:

1. **Создание лобби:**
   - `/start` → `🎮 Создать лобби`
   - Выберите ботов
   - Выберите режим (CM, AP, RD, SD, 1v1, etc.)
   - Выберите серию (BO1-BO5)

2. **Просмотр активных лобби:**
   - `📋 Список лобби`

3. **Управление ботами:**
   - `🤖 Управление ботами`

4. **Настройка расписания:**
   - `📅 Расписание`

## 🆘 Поддержка

Если возникли проблемы:
1. Проверьте логи: `dota2_real_bot.log`
2. Убедитесь, что все переменные в `.env` заполнены
3. Проверьте интернет-соединение
4. Убедитесь, что Dota 2 установлена и обновлена на аккаунтах

