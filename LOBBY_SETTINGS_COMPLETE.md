# ⚙️ Полные настройки лобби, которые устанавливает бот

## 📋 Все параметры в порядке создания

### 1️⃣ Базовая информация
```python
{
    'game_name': lobby_name,              # Название лобби (например: "wb cup 1")
    'pass_key': lobby_password,           # Пароль лобби
}
```

### 2️⃣ Серверный регион
```python
{
    'server_region': server_region,        # Регион сервера
}
```

**Маппинг регионов:**
- `'Stockholm'` → `8` (Stockholm регион)
- `'Europe West'` → `Europe`
- `'Russia'` → `Europe`
- `'US East'` → `USEast`
- `'US West'` → `USWest`

### 3️⃣ Режим игры
```python
{
    'game_mode': game_mode,               # Режим игры
}
```

**Маппинг режимов:**
- `'Captains Mode'` → `DOTA_GAMEMODE_CM`
- `'All Pick'` → `DOTA_GAMEMODE_AP`
- `'Captains Draft'` → `DOTA_GAMEMODE_CD`
- `'Mid Only'` → `DOTA_GAMEMODE_MO`
- `'1v1 Solo Mid'` → `DOTA_GAMEMODE_1V1MID`
- `'Random Draft'` → `DOTA_GAMEMODE_RD`
- `'Single Draft'` → `DOTA_GAMEMODE_SD`

### 4️⃣ Серия игр (BO)
```python
{
    'series_type': series_value,          # Серия игр (bo1, bo2, bo3, bo5)
    'radiant_series_wins': 0,            # Победы Radiant (начинается с 0)
    'dire_series_wins': 0,               # Победы Dire (начинается с 0)
}
```

**Маппинг серий:**
- `'bo1'` → `0` (Best of 1 - одна игра)
- `'bo2'` → `1` (Best of 2 - две игры)
- `'bo3'` → `2` (Best of 3 - до 2 побед)
- `'bo5'` → `3` (Best of 5 - до 3 побед)

### 5️⃣ Настройки турнира
```python
{
    'allow_spectating': False,            # Трансляция через DotaTV (выключена)
    'allow_cheats': False,               # Читы (выключены)
    'dota_tv_delay': 2,                  # Задержка трансляции (2 минуты)
    'fill_with_bots': False,             # Заполнение ботами (выключено)
    'leagueid': 18390,                   # ID турнира для отображения
}
```

### 6️⃣ Captains Mode настройки
```python
{
    'cm_pick': 1,                        # Подброс монетки для выбора стороны
                                         # 1 = автоматический подброс монетки
}
```

⚠️ **ВНИМАНИЕ:** Параметр `selection_priority` (ручной выбор порядка выбора) НЕ поддерживается API и поэтому удален из кода.

---

## 🎮 Как применяются настройки

### Шаг 1: Создание лобби
```python
dota.create_practice_lobby(
    password=lobby_password,
    options=options
)
```

### Шаг 2: Ожидание создания
```python
lobby_created.wait(timeout=60)  # Ждет до 60 секунд
```

### Шаг 3: Повторное применение настроек
```python
dota.config_practice_lobby(options=options)  # Для уверенности
gevent.sleep(0.5)  # Минимальная задержка
```

### Шаг 4: Присоединение бота
```python
# В слот канала трансляции
dota.join_practice_lobby_broadcast_channel(channel=1)

# В слот наблюдателя (team=4)
dota.join_practice_lobby_team(team=4)
```

---

## ⚠️ Важные наблюдения

### 1. Подброс монетки
В Captains Mode и Captains Draft бот устанавливает:
```python
'cm_pick': 1  # Автоматический подброс монетки
```

**ВАЖНО:** Бот повторно применяет настройки с `cm_pick: 1` **непосредственно перед запуском игры**, чтобы гарантировать что подброс монетки произойдёт.

Это означает что:
- ✅ Подброс монетки происходит **автоматически** при запуске игры
- ✅ Настройки применяются дважды: при создании лобби и перед запуском
- ❌ Бот **НЕ может** задать конкретную сторону для первого выбора
- ✅ Это стандартное поведение для турнирных лобби

### 2. Порядок выбора героев
В Captains Mode порядок выбора фиксированный:

**Фаза баннов:**
1. Radiant бан 1
2. Dire бан 2
3. Radiant бан 2
4. Dire бан 1
5. Radiant бан 2
6. Dire бан 2

**Фаза пиков:**
1. Radiant пик 1
2. Dire пик 2
3. Radiant пик 2
4. Dire пик 2
5. Radiant пик 2
6. Dire пик 1

❌ **НЕ настраивается** через API - это стандартный порядок.

### 3. Автозапуск
Текущая логика автозапуска:
```python
if radiant_players == 5 and dire_players == 5:
    dota.launch_practice_lobby()  # Запускается сразу!
```

⚠️ **ПРОБЛЕМА:** В Captains Mode игра может не запуститься если капитаны **НЕ назначены**!

---

## 🐛 Известные проблемы

### 1. Выбор порядка (selection_priority)
❌ **Параметр не поддерживается:**
```python
# ВЫЗЫВАЕТ ОШИБКУ:
'selection_priority': 0  # TypeError: Unexpected keyword argument 'selection_priority'
```

**Решение:** Удалено из кода, используется только `cm_pick: 1`.

### 2. Проверка капитанов
❌ **Текущий код НЕ проверяет:**
- Назначены ли капитаны команд
- Готовы ли команды к запуску

**Текущее поведение:**
- Запускается когда 10 игроков на местах
- НЕ проверяет наличие капитанов

⚠️ **Это может привести к сбою в Captains Mode!**

---

## 🔧 Что нужно улучшить

### 1. Проверка капитанов перед запуском
Для Captains Mode и Captains Draft нужно проверять:
```python
# Проверить есть ли капитаны
has_captains = any(member.is_captain for member in lobby.all_members)

if radiant_players == 5 and dire_players == 5 and has_captains:
    dota.launch_practice_lobby()
```

### 2. Проверка состояния лобби
Возможно стоит проверять:
```python
lobby_state = dota.lobby.state  # Неизвестно что там

if lobby_state == "ready":  # Если есть такое состояние
    dota.launch_practice_lobby()
```

---

## 📊 Итоговая таблица настроек

| Параметр | Значение | Описание |
|----------|----------|----------|
| `game_name` | Динамическое | Название лобби |
| `pass_key` | Генерируется | Пароль лобби |
| `server_region` | Stockholm/EU/US | Регион сервера |
| `game_mode` | CM/AP/CD/etc. | Режим игры |
| `series_type` | BO1/BO2/BO3/BO5 | Серия игр |
| `radiant_series_wins` | 0 | Победы Radiant |
| `dire_series_wins` | 0 | Победы Dire |
| `allow_spectating` | False | Трансляция |
| `allow_cheats` | False | Читы |
| `dota_tv_delay` | 2 | Задержка трансляции |
| `fill_with_bots` | False | Боты |
| `leagueid` | 18390 | ID турнира |
| `cm_pick` | 1 | Подброс монетки |

---

## 🎯 Рекомендации

1. ✅ **Текущие настройки работают** для большинства режимов
2. ⚠️ **Нужна проверка капитанов** для Captains Mode/Draft
3. ❌ **selection_priority НЕ поддерживается** - используйте cm_pick: 1
4. ✅ **Подброс монетки автоматический** - это нормально для турниров

---

Дата: 2025-11-03

