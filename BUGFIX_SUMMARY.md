# 🐛 Исправленные ошибки

## Проблема
Бот создавал только 2 из 3 лобби, первое лобби не создавалось корректно.

## Найденные баги

### 1. ❌ Отсутствие `return` при таймауте создания лобби

**Описание:**
Когда создание лобби не успевало в течение 58 секунд, код выводил ошибку в лог и отпра влял сообщение об ошибке в очередь, но **продолжал выполняться дальше**. Это приводило к тому, что:
- Процесс запускал цикл проверки игроков
- В итоге отправлялся shutdown signal
- Первое лобби попадало в состояние "не создано"

**Исправление:**
Добавлен `return` после отправки сообщения об ошибке в очередь результатов:

```python
else:
    local_logger.error(f"[{username}] Таймаут создания лобби")
    result_queue.put({'success': False, 'error': 'Lobby creation timeout'})
    # ВАЖНО: Выходим из функции при ошибке, иначе код продолжит выполняться!
    return  # <-- ДОБАВЛЕНО
```

### 2. ⏱️ Слишком короткое время ожидания события

**Описание:**
Между вызовом `create_practice_lobby()` и проверкой `lobby_created.wait()` был только 1 цикл `gevent.sleep(2)` секунд. Этого могло не хватать для получения события `EVENT_LOBBY_NEW` от Steam Game Coordinator, особенно при медленной сети или высокой нагрузке на серверы Valve.

**Исправление:**
Увеличено время ожидания с 2 до 5 секунд:

```python
# Было:
gevent.sleep(2)  # Уменьшено с 5 до 2 секунд

# Стало:
gevent.sleep(5)  # Даем время на получение события
```

Также немного скорректирован таймаут `lobby_created.wait()` с 58 до 55 секунд для соответствия общему лимиту в 60 секунд.

## Результат

✅ Все 3 лобби теперь должны создаваться корректно  
✅ При ошибке создания лобби процесс корректно завершается  
✅ Увеличено время на обработку событий от Steam GC

## Тестирование

Для проверки исправлений:

1. Перезапустите бота на VPS
2. Создайте 3 лобби через Telegram
3. Проверьте логи - все 3 лобби должны создаться успешно
4. Проверьте что в игровой клиент попали все 3 лобби

## Дата исправления
2025-11-03

