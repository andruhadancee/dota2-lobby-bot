# 🚀 Запуск на VPS

## Команды для запуска:

```bash
# 1. Перейдите в папку проекта
cd ~/dota2_lobby_bot

# 2. Проверьте что файлы на месте
ls -la

# 3. Проверьте .env файл
cat .env

# 4. Запустите бота
python main.py
```

## Если папка называется по-другому:

Найдите правильную папку:
```bash
find ~ -name "main.py" 2>/dev/null
find ~ -name "dota2_real_lobby_bot_v2.py" 2>/dev/null
```

## Проверка .env:

В `.env` должны быть:
```
TELEGRAM_BOT_TOKEN=...
ADMIN_IDS=...
```

## Запуск в фоне:

Чтобы бот работал после закрытия терминала:

```bash
# Вариант 1: nohup
nohup python main.py > bot.log 2>&1 &

# Вариант 2: screen
screen -S dota2bot
python main.py
# Нажмите Ctrl+A затем D для отключения

# Вариант 3: tmux
tmux new -s dota2bot
python main.py
# Нажмите Ctrl+B затем D для отключения
```

## Остановка:

```bash
# Найти процесс
ps aux | grep python

# Остановить
kill <PID>
# или
pkill -f "python main.py"
```

## Проверка логов:

```bash
tail -f dota2_real_bot.log
```

