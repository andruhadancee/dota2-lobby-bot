# 🚀 ИНСТРУКЦИЯ ПО ОБНОВЛЕНИЮ

## Что нужно сделать:

### 1️⃣ В папке проекта на вашем компьютере:

Двойной клик на файл: **`push_changes.bat`**

Или откройте терминал в папке проекта и введите:
```
push_changes.bat
```

Или вручную в терминале:
```
git add dota2_real_lobby_bot_v2.py
git commit -m "optimize: remove redundant sleeps and use events for faster lobby creation"
git push
```

---

### 2️⃣ На VPS (в Termius):

Откройте Termius, подключитесь к серверу и выполните:

```bash
cd ~/dota2_lobby_bot
bash update_vps.sh
```

Или по шагам:
```bash
cd ~/dota2_lobby_bot
git pull
source venv/bin/activate
python main.py
```

---

## ✅ Проверка

1. Откройте Telegram
2. Попросите бота создать 3 лобби
3. Проверьте что они создались быстро (около 1.5 минут вместо 2.5)

---

## 📝 Если что-то пошло не так

Остановите бота на VPS (Ctrl+C) и запустите заново:
```bash
cd ~/dota2_lobby_bot
source venv/bin/activate
python main.py
```

